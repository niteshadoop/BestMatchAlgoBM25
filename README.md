# BestMatchAlgoBM25
Best Match Algorithm BM25 (Best Matching 25) is a ranking algorithm used by search engines and information retrieval systems to determine how relevant a document is to a user's search query.
When a user searches for a keyword, BM25 scores all documents and ranks them based on relevance. Documents with higher scores appear higher in the search results
